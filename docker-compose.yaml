version: "3.9"

services:
  fdroid:
    build:
      dockerfile: Dockerfile
      context: .
    working_dir: /fdroid
    environment:
      - FDROID_REPO=fdroid/repo
    volumes:
      - ./fdroid:/fdroid
      - ./dist:/dist
